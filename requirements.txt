# Pomodoro Lock Dependencies
# These packages are required for the application to function properly

# GTK3 bindings for Python (for GUI components)
# Note: This is typically installed via system package manager
# python3-gi

# Process and system utilities (for activity detection)
psutil>=5.8.0

# X11 library bindings (for display management)
python-xlib>=0.29

# Desktop notifications
notify2>=0.3.1

# Additional dependencies that may be needed:
# - python3-gi (GTK3 bindings) - install via: sudo apt-get install python3-gi
# - python3-psutil - install via: sudo apt-get install python3-psutil
# - python3-xlib - install via: sudo apt-get install python3-xlib
# - python3-notify2 - install via: sudo apt-get install python3-notify2 